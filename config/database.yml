# for some reason, <<: *base is not working

base: &base
  #adapter: em_postgresql
  host: localhost
  username: <%= ENV['DBUSER'] || ENV['USER'] || "zambosa" %>
  password: <%= ENV['DBPASS'] || "" %>
  
development:
  adapter: em_postgresql
  host: localhost
  username: <%= ENV['DBUSER'] || ENV['USER'] || "zambosa" %>
  password: <%= ENV['DBPASS'] || "" %>
  database: zambosa_dev
  #pool: 20
  wait_timeout: 0.25

production:
  <<: *base
  database: zambosa_prod

staging:
  <<: *base
  database: zambosa_staging

test:
  <<: *base
  database: zambosa_test
