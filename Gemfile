source 'http://rubygems.org'

gem 'goliath', :git => 'https://github.com/postrank-labs/goliath.git'
gem 'em-synchrony', :git => 'https://github.com/igrigorik/em-synchrony.git'
gem 'eventmachine'
gem 'em-http-request', '>= 1.0.0.beta.3'
gem 'em-mongo'
gem 'yajl-ruby'
gem 'oauth'

# em_postgresql only works with activerecord ~>2 for now, this is OK, since we
# are only getting the app secret from partners table
gem 'postgres-pr', :require => 'postgres'

# forked from https://github.com/mperham/em_postgresql (only .gemspec has been added, so
# we can install it as a gem)
gem 'em_postgresql', '>= 0.4.0', :git => 'git@github.com:bruchu/em_postgresql.git'

gem 'rack-fiber_pool',  :require => 'rack/fiber_pool'

gem 'activerecord', '~> 2.3.5'

gem 'ruby-debug19'

