source 'http://rubygems.org'

gem 'goliath', :git => 'https://github.com/postrank-labs/goliath.git'
gem 'em-synchrony', :git => 'https://github.com/igrigorik/em-synchrony.git'
gem 'eventmachine'
#gem 'eventmachine', :git => 'https://github.com/bruchu/eventmachine.git'
#gem 'eventmachine', :path => File.join(File.dirname(__FILE__), '../../Code/eventmachine/')
gem 'em-http-request', '>= 1.0.0.beta.3'
gem 'em-mongo'
gem 'yajl-ruby'
gem 'oauth'

# em_postgresql only works with activerecord ~>2 for now, this is OK, since we
# are only getting the app secret from partners table
#gem 'postgres-pr'
#gem 'em_postgresql' , :path => File.join(File.dirname(__FILE__), '/vendor/gems/em_postgresql-0.3.0')
#gem 'em_pg', :path => File.join(File.dirname(__FILE__), '/vendor/gems/em_pg-0.0.1')

gem 'pg'
#gem 'em-postgresql-adapter', :git => 'https://github.com/leftbee/em-postgresql-adapter.git'
#gem 'em-postgresql-adapter', :path => File.join(File.dirname(__FILE__), '../../Code/em-postgresql-adapter/')
gem 'em-postgresql-adapter', :git => 'git@github.com:bruchu/em-postgresql-adapter.git'
gem 'rack-fiber_pool',  :require => 'rack/fiber_pool'

gem 'activerecord'

gem 'ruby-debug19'

#gem 'mysqlplus'
#gem 'em-mysqlplus'
